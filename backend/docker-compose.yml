services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: campus_agency
    ports:
      - "5433:5432"
    volumes:
      - campus_pgdata:/var/lib/postgresql/data

volumes:
  campus_pgdata: